<!-- Default dropend button -->
<div class="btn-group dropend">
  <a type="button" class="btn btn-secondary"aria-expanded="false" href="{{route('companies')}}">
    Companies
  </a>
</div>

<!-- Split dropend button -->
<div class="btn-group dropend">
  <a type="button" class="btn btn-secondary"  href="{{route('employees')}}">
    Employees
  </a>
</div>